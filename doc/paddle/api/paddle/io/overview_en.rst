.. _paddle_io_overview:

paddle.io
--------------

paddle.io contains APIs about input and output，such as load data API `Dataset` and `DataLoader`, and save/load model APIs `save_inference_model` and `load_inference_model`。
