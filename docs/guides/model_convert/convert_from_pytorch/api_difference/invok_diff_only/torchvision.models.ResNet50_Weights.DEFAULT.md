## [ 仅 API 调用方式不一致 ]torchvision.models.ResNet50_Weights.DEFAULT

### [torchvision.models.ResNet50_Weights.DEFAULT](https://pytorch.org/vision/stable/models/generated/ResNet50_Weights.html#torchvision.models.ResNet50_Weights.DEFAULT)

```python
torchvision.models.ResNet50_Weights.DEFAULT
```

两者功能一致，但调用方式不一致，具体如下：

### 转写示例

```python
## PyTorch 写法
mode = torchvision.models.ResNet50_Weights.DEFAULT

## Paddle 写法
mode = 'DEFAULT'
```
